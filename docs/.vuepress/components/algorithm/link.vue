<template>
    <div>
        <Content slot-key="title-add-two-numbers"/>
        <h4>给出两个 非空 的链表用来表示两个非负的整数。其中，它们各自的位数是按照 逆序 的方式存储的，并且它们的每个节点只能存储 一位 数字。
如果，我们将这两个数相加起来，则会返回一个新的链表来表示它们的和。您可以假设除了数字 0 之外，这两个数都不会以 0 开头。</h4>
        <p>示例</p>
        <p>输入：(2 -> 4 -> 3) + (5 -> 6 -> 4)
输出：7 -> 0 -> 8
原因：342 + 465 = 807</p>
        <Content slot-key="code-add-two-numbers1"/>
        <Content slot-key="title-swap-nodes"/>
        <h4>给定一个链表，两两交换其中相邻的节点，并返回交换后的链表。你不能只是单纯的改变节点内部的值，而是需要实际的进行节点交换。</h4>
        <p>示例</p>
        <p>输入：head = [1,2,3,4]
输出：[2,1,4,3]</p>
        <Content slot-key="code-swap-nodes1"/>
        <Content slot-key="title-add-two-numbers"/>
        <h4>给出两个 非空 的链表用来表示两个非负的整数。其中，它们各自的位数是按照 逆序 的方式存储的，并且它们的每个节点只能存储 一位 数字。
如果，我们将这两个数相加起来，则会返回一个新的链表来表示它们的和。您可以假设除了数字 0 之外，这两个数都不会以 0 开头。</h4>
        <p>示例</p>
        <p>输入：(2 -> 4 -> 3) + (5 -> 6 -> 4)
输出：7 -> 0 -> 8
原因：342 + 465 = 807</p>
        <Content slot-key="code-add-two-numbers1"/>
    </div>
</template>

<script>
    export default {
        
    }
</script>

<style lang="scss" scoped>

</style>